@if (WebhookRule is not null)
{
    <div class="d-flex justify-space-between align-center">
        <MudChip Variant="Variant.Text" Color="@(WebhookRule.Method.GetMethodColor())">@WebhookRule.MethodLabel</MudChip>
        <MudText>@WebhookRule.Path</MudText>
        <MudText>@WebhookRule.Description</MudText>
        <MudIcon Icon="@Icons.Material.Filled.Edit" Title="Edit" @onclick="@(async () => await EditAsync())" />
    </div>
}

@code {
    [Parameter]
    public WebhookRuleDto? WebhookRule { get; set; }

    [Parameter]
    public EventCallback OnEdit { get; set; }

    private async Task EditAsync()
    {
        await OnEdit.InvokeAsync();
    }
}
